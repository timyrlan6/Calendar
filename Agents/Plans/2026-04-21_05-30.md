# Запись на звонок — Итог рассуждений

**Дата:** 2026-04-21

## Цель проекта

Упрощенный сервис бронирования времени по мотивам Cal.com на Blazor с PostgreSQL.

## Технические требования

**Путь к проекту:** `C:\Projects\Calendar`

- PostgreSQL: Host=127.0.0.1, Database=Calendar, Username=postgres, Password=123str
- Версия Npgsql: 8.0.11 (Npgsql.EntityFrameworkCore.PostgreSQL)
- Миграция с SQL Server на PostgreSQL уже выполнена
- Удалён функционал TwoFactorAuthentication и PersonalData из UI

## Технические решения

### 1. GUID для публичной ссылки
- **Требование:** GUID генерируется клиентскими средствами C# через `Guid.CreateVersion7()`
- **Конфигурация EF:** `.ValueGeneratedNever()` — чтобы EF не перезаписывал значение из C#
- **Почему Version 7:** sequential GUID, оптимизирован для PostgreSQL (не фрагментирует индекс)

### 2. PostgreSQL 18 и UUID
- PostgreSQL 18.3 поддерживает встроенную функцию `uuidv7()`, но мы используем клиентскую генерацию через C#

### 3. Timezone
- Брать из браузера пользователя: `Intl.DateTimeFormat().resolvedOptions().timeZone`
- Хранить как IANA timezone string в ApplicationUser.TimeZoneId

### 4. BookingLinkId
- Генерируется при регистрации пользователя
- URL: `/booking/{guid}`

## Discoveries (из обсуждения)

- Проект: Blazor Server, .NET 8, PostgreSQL 18.3
- Существующая страница настроек: `Calendar\Components\Account\Pages\Manage\Index.razor`
- **Страница настроек:** расширить существующую `/Account/Manage/Index` (не создавать новую)

## Модель данных

### ApplicationUser (расширение)
- `BookingLinkId` — Guid, уникальный, генерируется при регистрации
- `TimeZoneId` — IANA timezone (string)
- 14 полей для рабочих часов (Mon-Sun Start/End, где null = не работает)

### Booking
- `Id` — Guid (PK)
- `OwnerId` — string (FK на ApplicationUser)
- `GuestName` — string
- `GuestEmail` — string
- `Notes` — string?
- `StartTimeUtc` — DateTime
- `EndTimeUtc` — DateTime
- `GuestTimeZoneId` — string
- `CreatedAtUtc` — DateTime

### EventType
Не нужен — always 30 minutes.

## Принятые решения

### 1. Concurrency (EXCLUDE constraint)
Для гарантии отсутствия пересечений бронирований:
```sql
ALTER TABLE "Bookings" ADD CONSTRAINT "no_overlap" EXCLUDE USING gist (
    "OwnerId" WITH =,
    tstzrange("StartTimeUtc", "EndTimeUtc") WITH &&
);
```

### 2. Публичная ссылка
- Используется GUID (`BookingLinkId`), генерируемый при регистрации пользователя
- URL: `/booking/{guid}`
- Несуществующий GUID → 404
- Ссылка отображается в настройках профиля

### 3. Алгоритм расчёта слотов
(требует реализации)

## Выполненные задачи

| Задача | Статус |
|--------|--------|
| Миграция на PostgreSQL | ✅ |
| Удаление TwoFactorAuthentication и PersonalData из UI | ✅ |
| Проектирование модели данных | ✅ |
| Выбор механизма concurrency (EXCLUDE constraint) | ✅ |
| Определение GUID для публичной ссылки | ✅ |
| Определение способа генерации GUID (C# Version 7) | ✅ |
| Определение расширения страницы настроек | ✅ |

## Предстоит создать

1. Расширить `ApplicationUser` полями (BookingLinkId, TimeZoneId, Mon-Sun Start/End)
2. Создать модель `Booking` в `Data/`
3. Настроить `OnModelCreating` с `.ValueGeneratedNever()` для BookingLinkId
4. Расширить страницуManage/Index.razor для отображения настроек
5. Создать миграцию с EXCLUDE constraint
6. Создать страницу `/booking/{guid}`
7. Реализовать генерацию BookingLinkId при регистрации
8. Компонент календаря для выбора слотов

## Следующие шаги

1. Добавить поля в ApplicationUser + создать Booking модель
2. Создать миграцию с EXCLUDE constraint
3. Реализовать генерацию BookingLinkId при регистрации
4. Реализовать страницу настроек (Manage/Index.razor)
5. Реализовать публичную страницу бронирования

## Relevant files

- `C:\Projects\Calendar\Calendar\Data\ApplicationUser.cs` — нужно расширить
- `C:\Projects\Calendar\Calendar\Data\ApplicationDbContext.cs` — нужно добавить OnModelCreating
- `C:\Projects\Calendar\Calendar\Calendar\Components\Account\Pages\Manage\Index.razor` — нужно расширить
- `C:\Projects\Calendar\Calendar\Calendar.csproj` — для проверки версий пакетов