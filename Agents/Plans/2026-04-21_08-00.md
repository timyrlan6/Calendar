# Запись на звонок — Итог рассуждений

**Дата:** 2026-04-21

## Цель проекта

Упрощенный сервис бронирования времени по мотиву Cal.com на Blazor с PostgreSQL.

## Технические требования

**Путь к проекту:** `C:\Projects\Calendar`

- PostgreSQL: Host=127.0.0.1, Database=Calendar, Username=postgres, Password=123str
- Npgsql: 8.0.11 (Npgsql.EntityFrameworkCore.PostgreSQL)
- .NET 8.0

---

## ✅ Принятые решения (утверждено)

> Эти решения зафиксированы и НЕ подлежат пересмотру.

| Решение | Значение |
|---------|----------|
| EventType | Поддержка разных типов событий с разной длительностью |
| GUID | `Guid.CreateVersion7()` на клиенте + `.ValueGeneratedNever()` в EF |
| EXCLUDE constraint | `BookingLinkId` + `tstzrange(StartTimeUtc, EndTimeUtc) WITH &&` |
| Timezone | IANA string из браузера (`Intl.DateTimeFormat().resolvedOptions().timeZone`) |
| Публичная ссылка | URL: `/booking/{guid}`, несуществующий GUID → 404 |
| Рабочие часы | 14 полей TimeOnly (Mon-Sun Start/End) |
| Register | Создаёт BookingLinkId + EventType (30 min, default) в одной транзакции |
| DateTime | Всегда UTC (`DateTimeKind.Utc`), НЕ `Unspecified` |
| Строка подключения | Брать из appsettings.json проекта (НЕ из внешних инструментов) |

### Текстовые поля

Все текстовые поля (string), кроме системных (GUID, ID, FK), должны иметь ограничение:
- **256** — для маленьких полей (Name, Email, TimeZoneId и т.п.)
- **1024** — для больших полей (Notes, Description)

Без ограничений — оговаривается отдельно.

### Вопросы БОЛЬШЕ НЕ задавать

- ❌ "Нужен ли EventType?" — ДА, подтверждено
- ❌ "Какую колонку для EXCLUDE?" — BookingLinkId, подтверждено
- ❌ "Какой GUID использовать?" — Version7, подтверждено

---

## Структура данных

### ApplicationUser (расширение)
```csharp
public Guid BookingLinkId { get; set; }         // публичная ссылка
public string TimeZoneId { get; set; } = "UTC"; // IANA timezone

public TimeOnly MonStart { get; set; }        // понедельник
public TimeOnly MonEnd { get; set; }
// TueStart/End, WedStart/End, ThuStart/End, FriStart/End, SatStart/End, SunStart/End

public ICollection<EventType> EventTypes { get; set; }
public ICollection<Booking> Bookings { get; set; }
```

### EventType (справочник типов событий)
```csharp
public long Id { get; set; }                   // PK auto-increment
public string OwnerId { get; set; } = null!;    // FK → ApplicationUser
public Guid BookingLinkId { get; set; }         // для индекса
public string Name { get; set; } = null!;      // MaxLength(256)
public TimeOnly Duration { get; set; }       // meeting duration
public bool IsDefault { get; set; }           // true

public ApplicationUser Owner { get; set; } = null!;
```

### Booking
```csharp
public Guid Id { get; set; }                  // PK
public Guid BookingLinkId { get; set; }       // FK + индекс
public string OwnerId { get; set; } = null!; // FK → ApplicationUser
public string GuestName { get; set; } = null!;   // MaxLength(256)
public string GuestEmail { get; set; } = null!;  // MaxLength(256)
public string? Notes { get; set; }              // MaxLength(1024)
public DateTime StartTimeUtc { get; set; }     // UTC
public DateTime EndTimeUtc { get; set; }       // UTC
public string EventTypeName { get; set; } = null!;   // MaxLength(256)
public TimeOnly EventDuration { get; set; }
public string GuestTimeZoneId { get; set; } = null!; // MaxLength(256)
public DateTime CreatedAtUtc { get; set; }    // UTC

public ApplicationUser Owner { get; set; } = null!;
```

---

## Fluent API (ApplicationDbContext)

```csharp
builder.Entity<ApplicationUser>(entity =>
{
    entity.HasIndex(e => e.BookingLinkId).IsUnique();
    entity.Property(e => e.BookingLinkId).ValueGeneratedNever();
});

builder.Entity<EventType>(entity =>
{
    entity.HasKey(e => e.Id);
    entity.Property(e => e.Id).ValueGeneratedOnAdd();
    entity.HasIndex(e => e.OwnerId);
    entity.HasIndex(e => e.BookingLinkId);
    entity.HasOne(e => e.Owner)
        .WithMany(u => u.EventTypes)
        .HasForeignKey(e => e.OwnerId)
        .OnDelete(DeleteBehavior.Cascade);
});

builder.Entity<Booking>(entity =>
{
    entity.HasKey(e => e.Id);
    entity.HasIndex(e => e.BookingLinkId);
    entity.HasOne(e => e.Owner)
        .WithMany(u => u.Bookings)
        .HasForeignKey(e => e.OwnerId)
        .OnDelete(DeleteBehavior.Cascade);
});
```

---

## Миграции

| Дата | Описание |
|------|----------|
| 2026-04-19 | InitialCreate |
| 2026-04-21 | AddBookingFields |

Миграции созданы и применены к БД `Calendar`.

**EXCLUDE constraint** — будет добавлен отдельной миграцией:
```sql
ALTER TABLE "Bookings" ADD CONSTRAINT "no_overlap" EXCLUDE USING gist (
    "BookingLinkId" WITH =,
    tstzrange("StartTimeUtc", "EndTimeUtc") WITH &&
);
```

---

## Файлы проекта

| Файл | Статус |
|------|--------|
| `Calendar/Data/ApplicationUser.cs` | ✅ расширен |
| `Calendar/Data/EventType.cs` | ✅ создан |
| `Calendar/Data/Booking.cs` | ✅ создан |
| `Calendar/Data/ApplicationDbContext.cs` | ✅ Fluent API |
| `Calendar/Data/Migrations/` | ✅ миграции созданы |
| `Calendar/RULES.md` | ✅ создан |

---

## Порядок реализации (выполнено)

1. ✅ Расширить ApplicationUser
2. ✅ Создать EventType
3. ✅ Создать Booking
4. ✅ Настроить ApplicationDbContext
5. ✅ Создать миграции
6. ⏳ Реализовать Register.razor (BookingLinkId + EventType)
7. ⏳ Расширить Manage/Index.razor
8. ⏳ Страница /booking/{guid}
9. ⏳ Добавить EXCLUDE constraint